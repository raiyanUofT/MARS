Step 1 - Create a virtual environment in Python3

Step 2 - Run the following command in the terminal: pip install -r requirements.txt

Step 3 - Run the following command in the terminal: bash run_all_training.sh
